# Pinned with lower bounds; pip will pick compatible upper versions.
# Re-pin to exact versions before deploying to Cloud Run.

google-genai>=0.3.0
chromadb>=0.5.0
pypdf>=5.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
streamlit>=1.40.0
python-dotenv>=1.0.0
pydantic>=2.9.0
requests>=2.32.0
